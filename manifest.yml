---
applications:
- name: simple-mongodb-rest-app
  host: simple-mongodb-rest-app
  memory: 1GB
  instances: 1
  path: build/libs/micropcf-exampleApp.war
  buildpack: java_buildpack
  services:
  - p-mongodb
  env:
      SPRING_PROFILES_ACTIVE: cloud
